define( [
        "backbone",
        "underscore"
    ],
    function ( backbone, _ )
    {
        "use strict";

        var BackboneModule = Backbone.noConflict();

        return BackboneModule;
    } );